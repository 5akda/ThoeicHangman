<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_vocab"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/midBlack">

    <fragment
        android:id="@+id/vocabFragment"
        android:name="tech.parzival48.thoeic.ui.vocab.VocabFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:label="@string/vocab_fragment"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph_vocab"
        tools:layout="@layout/fragment_vocab" />

</FrameLayout>